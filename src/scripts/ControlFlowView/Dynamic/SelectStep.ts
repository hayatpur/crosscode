export function selectStep(stepId: string) {
    // const step = Steps.get(stepId)
    // step.controlFlowRenderer.element.classList.add('is-focused')
    // step.sourceCodeRenderer.element.classList.add('is-focused')
    // step.isSelected = true
    // // If action is consumed, update consumed select()
    // const abbreviationInfo = Abbreviation.getConsumedAbyss(this.stepId)
    // if (abbreviationInfo) {
    //     updateAbyssSelection(abbreviationInfo.id)
    // }
}

export function deselectStep(stepId: string) {
    // const step = Steps.get(stepId)
    // step.controlFlowRenderer.element.classList.remove('is-focused')
    // action.element.classList.remove('is-focused')
    // this.isSelected = false
    // // If action is consumed, update consumed select()
    // const abbreviationInfo = getConsumedAbyss(this.stepId)
    // if (abbreviationInfo) {
    //     updateAbyssSelection(abbreviationInfo.id)
    // }
}
